{
  "name": "nibby-gelato",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "check": "astro check",
    "assets": "node scripts/generate-social.mjs",
    "build": "npm run check && npm run assets && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@fontsource/dm-sans": "^5.3.0",
    "@fontsource/fraunces": "^5.3.0",
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "@axe-core/playwright": "latest",
    "@playwright/test": "latest",
    "@types/node": "^26.5.0",
    "sharp": "^0.35.4",
    "typescript": "latest",
    "vitest": "latest"
  }
}
